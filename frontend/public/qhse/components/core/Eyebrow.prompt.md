Mono uppercase kicker label above section headings — the brand's "technical voice".

```jsx
<Eyebrow>核心能力 / Capabilities</Eyebrow>
<Eyebrow tick={false} tone="muted">02 — Workflow</Eyebrow>
```

Props: `tick` (leading lime mark, default true), `tone` (`accent | muted`).
